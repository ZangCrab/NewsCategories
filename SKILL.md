# News Categories SKILL

> 版本：**2.4.0**
>
> 采用“主题 + 实体”双轨制：**主题分类**决定“这篇文章是什么体裁/话题”，**实体标签**决定“这篇文章关于谁/什么比赛”。两者互相独立，但必须组合使用——任何一篇收录的内容，至少拥有 **1 个主题分类 + 1 个实体标签**（通常是赛事或球队）。

## 变更记录

- **2.4.0**：输出新增选填 `matches`（比赛关系列表，主客必填），把“谁 vs 谁”结构化；明确主客判定（vs 左边即主队、中性场地用官方行政主客）、主次优先（只关联主比赛）、无上限且与实体列表独立。
- **2.3.0**：命名规范收紧为“官方中文全称”（禁用简称/缩写），《实体同义词表》球队/赛事/机构三节全部翻转为全称作规范名；明确“全称原则”与语言一致性（当前中文全称，后续可切语言）。
- **2.2.0**：输出新增选填 `transfers`（球员转会关系）与 `coach_changes`（教练更迭关系）两个列表，把“谁、从哪里、到哪里”结构化，弥补实体扁平列表无法表达人事流动关系的不足。
- **2.1.0**：新增实体数量截断排序规则（《通用规则.md》第 4 条）；命名规范改为“确定性输出规则”（中文 / 全名 / 官方名），同义词表降级为可选参考；明确范围边界（程序化归一化属下游系统、权威规范名来源待定）。
- **2.0.0**：`type` 数字代码改为语义枚举（`event/team/player/coach/organization`）；子类锁定为闭包枚举并以《主题.md》为唯一事实来源；新增 `included` 不收录分支与结构化 `meta` 字段；`reason` 升级为规则引用；新增《实体同义词表.md》《返回结构.schema.json》《金标准样本.md》；组合规则粒度下沉到子类。
- **1.0.0**：初始版本（主题/实体双轨、正反例收录标准、边界打分表）。

## 输入（Input）

打标输入为一篇文章，字段如下（`title`、`body` 必填，其余选填）：

```json
{
  "title": "文章标题",
  "lead": "导语/摘要（可选）",
  "body": "正文全文",
  "source": "来源（可选）",
  "published_at": "发布时间（可选）"
}
```

> 无标题时，“标题优先 + 导语辅助”的判定降级为“导语/正文前两段优先”。

## 输出（Output）

输出结构见《返回结构.md》，机器校验见《返回结构.schema.json》。核心：`included`（是否收录）→ `theme`（单一主类 + 1–2 子类）→ `entity`（实体列表）→ `transfers`/`coach_changes`/`matches`（选填关系列表）→ `reason`（必填理由）。

## 各文件职责

- **主题.md** —— 主题（体裁）主类与子类的**唯一事实来源**（闭包枚举），管“这是什么文章”。
- **实体.md** —— 实体类型（语义枚举）、命名规范（第一层确定性规则）与消歧（结构化 `meta`），管“这篇关于谁/什么比赛”。
- **组合规则.md** —— “主题 → 子类 → 必打/可选实体”的约束（粒度下沉到子类），管实体维度的下限。
- **通用规则.md** —— 跨主题的全局硬规则（收录判定、最少标签数、上限与截断排序、去重、命名规范、枚举合法）。
- **收录标准.md** —— “主题归属 + 边界判定”的正反例对照，管主题维度的裁决，是边界冲突时的最终依据。
- **返回结构.md** —— 打标结果的输出格式（含 `included` 分支、必填理由 `reason`、选填 `transfers`/`coach_changes`/`matches` 关系列表）。
- **实体同义词表.md** —— 高频实体规范名 + 别名（**可选参考，非强制**）。
- **返回结构.schema.json** —— 输出的机器可执行 JSON Schema（硬校验）。
- **金标准样本.md** —— 端到端标注样本，用于 few-shot 与回归评测。

## 打标流程

1. 判 `included`（是否收录，见《通用规则.md》第 1 条）。
2. 判主类 → 选子类（见《主题.md》《收录标准.md》）。
3. 打实体标签（见《组合规则.md》必打/可选，`name` 遵循《实体.md》命名规范）。
4. 写 `reason`（引用规则锚点）。
5. 输出跑《返回结构.schema.json》硬校验。

## 范围边界

- **本 SKILL 负责**：单篇文章的主题分类、实体打标、命名规范（第一层确定性规则）、输出结构。
- **不属于本 SKILL**：
  - 跨文章的实体归一化 / 去重 / 对齐实体 ID（程序化归一，由下游系统负责）；
  - “权威规范名来源”（第二层，尚未确定，待定后回填《实体.md》第 0 节与《实体同义词表.md》）。

## 目录

1. [主题](主题.md) —— 主题（体裁）主类与子类闭包枚举
2. [实体](实体.md) —— 实体类型与消歧
3. [组合规则](组合规则.md) —— 主题 → 子类 → 必打/可选实体
4. [通用规则](通用规则.md) —— 跨主题全局硬规则
5. [收录标准](收录标准.md) —— 主题归属与边界判定
6. [返回结构](返回结构.md) —— 输出格式（含不收录分支与 reason）
7. [实体同义词表](实体同义词表.md) —— 高频实体规范名参考（可选）
8. [返回结构.schema.json](返回结构.schema.json) —— 输出硬校验
9. [金标准样本](金标准样本.md) —— few-shot 与回归评测样本
