# 通用规则

> 跨主题的全局硬规则。违反任一条即视为打标不合格。

## 收录门槛

1. **收录判定**：每篇文章先判 `included`（是否收录）。不收录（`included = false`）的情况包括但不限于：
   - 非足球/非体育内容；
   - 不构成新闻的内容（如“球队发布伤病通告，全员健康”）；
   - 广告、硬广软文、纯引流内容。
   - 不收录时 `theme`、`entity` 置空（`null`），`reason` 必填并说明原因。

2. **收录则必打**：`included = true` 时，至少 **1 个主题分类 + 至少 1 个实体标签**（通常是赛事或球队）。

## 标签约束

3. **双方必打**：涉及双方的（比赛、转会谈判），必须同时打双方实体标签（如两队、两球员）。

4. **数量上限与截断**：一篇文章的实体标签最多 **15 个**。超出时按“第一信息价值”从高到低截断，排序如下（从第 4 级开始丢弃、逐级向上）：

   1. **必打标签**（《组合规则.md》标注“必打”的）——绝不截断；
   2. **核心论断对象**——文章的核心论断主体：被预测夺冠/前四/降级/获奖的球队或球员、被报道的核心人物；注意区分“被预测的对象”与“预测理由里顺带提到的配角”；
   3. **显著提及**——标题或导语出现，或正文反复出现；
   4. **边缘提及**——正文一次带过、仅作背景/举例的实体；
   5. 同层并列无法区分时，按类型 tiebreaker：球队 > 球员 > 教练 > 机构。

   被丢弃的实体必须在 `reason` 末尾列出。

5. **不重复**：同一实体标签在一篇文章中只能出现一次（`type + name` 相同视为重复）。

6. **命名规范**：实体 `name` 遵循《实体.md》第 0 节的命名规范（中文 / 全名 / 官方名）；《实体同义词表.md》仅作可选参考，非强制。

7. **枚举合法**：`theme.name` 九选一、`theme.sub` 必须来自该主类的子类枚举（见《主题.md》）、`entity[].type` 必须来自语义枚举（见《实体.md》）。三者均禁止自造值。
