# 实体

> 实体标签回答“这篇内容关于谁/什么比赛”，与主题（体裁）互相独立，必须组合使用。

## 0. 命名规范（确定性输出规则）

实体 `name` 的写法遵循以下硬规则，与原文语言无关：

1. **语言**：一律输出简体中文，不输出英文原词、外文缩写或拼音。
2. **人名（球员/教练）**：一律用**完整音译名**（名·姓），禁用简称、昵称、绰号（如用“埃尔林·哈兰德”而非“哈兰德”、“克里斯蒂亚诺·罗纳尔多”而非“C罗”）。
3. **球队/赛事/机构**：一律用**官方中文全称**，禁用简称/缩写/球迷昵称（如“国际米兰”而非“国米”、“曼彻斯特联”而非“曼联”、“皇家马德里”而非“皇马”；不用“红军”“蓝军”等昵称）。
4. **赛季**：固定 `YYYY-YY`（见下方赛事标签）。

> **全称原则**：实体 `name` 一律用**全称**而非简称——全称最权威、最官方，且简称的写法标准不统一（如“国际米兰”可被简写为“国米”、“曼彻斯特联”可被简写为“曼联”）。测试阶段统一用中文全称；后续如需切换语言，只换语言、不改“全称”原则。

> **待定（第二层）**：“完整音译名”“官方中文全称”的**具体规范写法以哪个权威来源为准，尚未确定**。确定前，`name` 优先参考《实体同义词表.md》（可选参考，非强制）；表内未收录的实体按上述四条规则用最常见的中文写法输出，并保证**同一篇文章内写法一致**。
>
> **范围说明（第三层）**：跨文章的实体归一化（同义词聚类、去重、对齐实体 ID）由下游系统负责，**不在本 SKILL 范围内**。本 SKILL 只保证单篇内的命名规则与一致性。

## 通用消歧要求

- 同名、跨届、跨项目、跨级别的实体，必须用 `meta` 字段消除歧义（见“meta 结构化消歧字段”）。
- 同一篇文章内，同一实体的写法必须统一（一律用全称，全文一致）。

## 实体类型（type 语义枚举）

| type | 含义 | 原数字代码（向后兼容） |
| --- | --- | --- |
| `event` | 赛事（联赛/杯赛/比赛） | `1` |
| `team` | 球队（俱乐部/国家队/代表队） | `2` |
| `player` | 球员 | `3` |
| `coach` | 教练 | `4` |
| `organization` | 机构/行政主体 | `5` |

> 语义枚举是唯一规范写法；原数字代码仅用于与既有数据库映射，打标输出中不使用。

## meta 结构化消歧字段（按 type 选填）

`remark` 已拆分为结构化字段 `meta`（对象，所有字段选填）；只有结构无法表达的信息才回退到 `remark` 自由文本。各 type 可用字段如下：

| type | meta 可用字段 | 字段含义 |
| --- | --- | --- |
| `event` | `season` / `category` | `season`=赛季，格式 `2025-26`；`category`=跨项目注明（女足 / U21 / U19 / 五人制） |
| `team` | `category` / `city` / `country` | `category`=女足/梯队/青年队与男足一线队区分；`city`/`country`=重名球队写城市或国家 |
| `player` | `club` / `country` / `birth_year` / `retired` | `club`=现役所属俱乐部；`country`=国家队语境；`birth_year`=同名球员；`retired`=已退役（true） |
| `coach` | `role` / `team` | `role`=职位（主教练/助教/门将教练等）；`team`=所属球队 |
| `organization` | `full_name` / `level` | `full_name`=机构全称；`level`=层级（国家级/洲际/国际） |

## 各类型说明

### 1. 赛事标签（`event`）

- `meta.season` 记录赛季，格式 `2025-26`、`2026-27`。
- 跨项目必须用 `meta.category` 注明：女足、青年队（U21/U19）、五人制等。
- 默认“英超”“西甲”指男子顶级联赛；女子赛事需写明，如“女子超级联赛（WSL）”。

### 2. 球队标签（`team`）

- 俱乐部与国家队是两个独立维度，可同时打（同一球员既属俱乐部又属国家队时）。
- 女足/梯队/青年队与男足一线队区分，`meta.category` 注明“女足”“U21”。
- 重名球队，`meta.city` 或 `meta.country` 写城市或国家（如多个“竞技”“联队”）。

### 3. 球员标签（`player`）

- 用全名（规范名）；消歧字段三选一或组合：`club`（现役所属俱乐部）、`country`（国家队语境）、`birth_year`（同名球员）。
- 退役球员仍打球员标签，`meta.retired = true`。
- 球员兼任教练/临时教练：按语境选——讲执教打 `coach`，讲球员身份打 `player`，两者兼有可同时打。

### 4. 教练标签（`coach`）

- 全名；教练组成员（助教、门将教练等）也算教练标签，`meta.role` 注明职位。
- 球员兼任教练：见“3. 球员标签”。

### 5. 机构/行政主体标签（`organization`）

- 定义：足协、联盟、管理机构，如英格兰足球总会（FA）、欧洲足球协会联盟（UEFA）、国际足球联合会（FIFA）、英格兰足球超级联赛、西班牙职业足球联盟、纪律委员会等。
- 用于“行政与管理”主题下无具体赛事/球队的主体（规则变更、纪律处分、官方公告）。
- `meta.full_name` 注明机构全称，`meta.level` 注明层级（国家级/洲际/国际）。
