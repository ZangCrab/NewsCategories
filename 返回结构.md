# 返回结构

> 打标结果的输出结构：`theme` 只含一个主类，`entity` 为实体列表，`reason` 为打标理由（必填、不能为空）。

## 字段说明

### 顶层字段

- **`theme`** —— object，必填。主题分类，含 `name` 与 `sub`。
- **`entity`** —— array，必填。实体标签列表，每项含 `type`、`name`、`remark`。
- **`reason`** —— string，必填。打标理由，说明“为什么这样判”，不能为空。

### theme 子字段

- **`theme.name`** —— string，必填。主题主类，九选一（见《主题.md》）。
- **`theme.sub`** —— string[]，必填。子类，1-2 个。

### entity 子字段

- **`entity[].type`** —— string，必填。实体类型代码（字符串），见下方映射。
- **`entity[].name`** —— string，必填。实体名称（全名）。
- **`entity[].remark`** —— string，选填。消歧备注，见《实体.md》；无歧义时省略。

## reason 说明

`reason` 为**必填**、**不能为空**，是打标结果的判断依据。一条合格的理由应包含：

1. **主类判定依据** —— 引用哪条规则（如“第一信息价值”、“标题优先 + 导语辅助”或某条正反例判据）。
2. **关键实体选择逻辑** —— 为什么打出这些实体标签。

## type 代码映射

- `1` → 赛事
- `2` → 球队
- `3` → 球员
- `4` → 教练
- `5` → 机构/行政主体

## 示例

```json
{
  "theme": {
    "name": "深度分析",
    "sub": ["战术拆解"]
  },
  "entity": [
    { "type": "1", "name": "英超", "remark": "2026-27" },
    { "type": "2", "name": "阿森纳" },
    { "type": "2", "name": "曼城" },
    { "type": "3", "name": "大卫·拉亚·马丁", "remark": "阿森纳，西班牙" },
    { "type": "4", "name": "米克尔·阿尔特塔", "remark": "主教练" }
  ],
  "reason": "因为核心内容是主教练“米克尔·阿尔特塔”对“阿森纳 vs 曼城”比赛的战术拆解，属于典型的“战术拆解”，内容重点提到了门将“大卫·拉亚·马丁”"
}
```

> 实体数量、不重复等校验约束见《通用规则.md》。
