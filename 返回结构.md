# 返回结构

> 打标结果的输出结构。`included` 决定是否收录；收录时 `theme` 只含一个主类、`entities` 为实体列表、`transfers`/`coach_changes`/`matches` 为选填关系列表、`reason` 为必填理由（不能为空）、`source` 为选填消息来源（无则省略）。机器可执行校验见《返回结构.schema.json》。

## 字段说明

### 顶层字段

- **`included`** —— boolean，必填。是否收录。`false` 表示噪声/非新闻/非足球内容，此时 `theme`、`entities` 为 `null`。
- **`theme`** —— object | null，`included = true` 时必填。主题分类，含 `name` 与 `sub`。
- **`entities`** —— array | null，`included = true` 时必填（至少 1 项）。实体标签列表，每项含 `type`、`name`、`importance`、`meta`（选填）、`remark`（选填）。
- **`transfers`** —— array，选填。转会关系列表，仅 `theme.name = 转会与合同` 且子类为 转入转出/租借/续约/解约退役 时出现；每项含 `type`、`player`、`from`、`to`（选填）、`remark`（选填），见下方“transfers 说明”。
- **`coach_changes`** —— array，选填。教练更迭关系列表，仅 `theme.name = 转会与合同` 且子类含 `教练更迭` 时出现；每项含 `type`、`coach`、`team`、`role`（选填）、`remark`（选填），见下方“coach_changes 说明”。
- **`matches`** —— array，选填。比赛关系列表，内容能确定主队与客队时出现（不限主题、无上限、与 `entities` 独立）；每项含 `home`、`away`、`event`（选填）、`referee`（选填）、`remark`（选填），见下方“matches 说明”。
- **`source`** —— object，选填。消息来源，从标题/正文中提取（见下方“source 说明”）；无来源时省略。含 `type`（报道方类型枚举）、`reporter`（报道方名称）、`quote`（原文原句）三个必填字段。
- **`reason`** —— string，必填、不能为空。打标理由，说明“为什么这样判”。

### theme 子字段

- **`theme.name`** —— string，必填。主题主类，九选一（见《主题.md》）。
- **`theme.sub`** —— string[]，必填。子类，1–2 个，必须来自该主类的子类枚举（见《主题.md》）。

### entities 子字段

- **`entities[].type`** —— string，必填。实体类型，语义枚举：`event` / `team` / `player` / `coach` / `organization` / `referee` / `executive` / `agent`。
- **`entities[].name`** —— string，必填。实体名称，遵循命名规范（见《实体.md》第 0 节；《实体同义词表.md》仅作可选参考）。
- **`entities[].importance`** —— string，必填。重要级别：`required`（第一优先级）/ `core`（核心论断对象）/ `significant`（显著提及），对应《通用规则.md》第 4 条前三级；第 4 级边缘提及不收录、无此字段。
- **`entities[].meta`** —— object，选填。结构化消歧字段，按 type 取值（见《实体.md》），无歧义时省略。
- **`entities[].remark`** —— string，选填。自由文本兜底，仅当 `meta` 无法表达时使用。

## reason 说明

`reason` 为**必填**、**不能为空**，是打标结果的判断依据。一条合格的理由应包含两部分，且**引用规则锚点**（而非仅复述判断）：

1. **主类判定依据** —— 引用命中的规则锚点，格式之一：
   - `《收录标准.md》<主类>·<子类>·正面示例`
   - `《收录标准.md》第二部分·优先级打分表`
   - `《收录标准.md》第二部分·多主类归属规则`
2. **实体选择逻辑** —— 说明为什么打出这些实体，引用《组合规则.md》的优先/可选。

不收录时，`reason` 说明为何不收录（引用《通用规则.md》第 1 条的情形）。

## source 说明

`source` 为**选填**，是从标题/正文中提取的**消息来源**，用于标注“这则消息是谁报道/发布的”。它是一个对象，含三个必填字段：

```json
{
  "type": "媒体与记者",
  "reporter": "罗马诺",
  "quote": "据知名记者罗马诺消息，奥斯梅恩将以 7500 万欧转会切尔西"
}
```

### 三个字段

**`type`** —— 报道方类型（枚举，4 值）。**看报道方的身份（谁说的），不看消息内容**：

| type | 报道方身份（原文定性） | 例子 |
| --- | --- | --- |
| 官方与当事人 | 官方机构，或新闻当事人本人（球员/教练/管理层） | 曼联官方声明、C罗本人采访 |
| 媒体与记者 | 专业媒体机构或记者 | 每日邮报、队报、罗马诺 |
| 爆料与泄密 | 掌握内幕的第三方个体（非记者、非当事人本人） | “队内人士透露”、女友爆料、匿名线人 |
| 民间与聚合 | 普通网友/球迷/自媒体/聚合平台（不宣称内幕） | 虎扑网友、微博营销号 |

**`reporter`** —— 报道方名称，自由文本（人名/机构名/网友等，不可枚举）。

**`quote`** —— 原文原句：正文中写明该来源的那句话，一字不差。**引用不出来 → 说明没有来源 → 省略 source**（这是防臆造的硬约束）。

### 规则

- **与 `entities` 无关**：source 标注“谁发的”，entities 标注“讲的是谁”，两个维度独立、互不影响（官方既可进 `entities`、也可进 `source`）。
- **只在原文明确写出报道方时填写**：标题或正文出现“据……报道/消息”“……爆料”“……称”，或当事人本人直接发言（如“C罗说”“C罗专访”）。
- **没有来源则省略**：原文未提及任何报道方时，**不输出 `source` 字段**（而非空对象）。
- **禁止臆造**：不得根据常识、上下文或实体名推断报道方；`quote` 必须是原文白纸黑字写出来的。

## transfers 说明

`transfers` 为**选填**，仅在 `theme.name = 转会与合同` 且子类为 转入转出 / 租借 / 续约 / 解约退役 时出现。它把“谁、从哪里、到哪里”的转会关系结构化，弥补 `entities` 扁平列表无法表达“主角与去向”的不足。

- **`type`** —— string，必填。转会类型：`转入转出` / `租借` / `续约` / `解约退役`（与子类枚举一致，不含教练更迭）。
- **`player`** —— string，必填。球员名（全名），取值必须与 `entities` 中对应球员的 `name` 一致。
- **`from`** —— string，必填。球员所在/原俱乐部：转入转出、租借=现俱乐部；续约=续约俱乐部；解约/退役=原俱乐部。
- **`to`** —— string，选填。目标俱乐部：仅 `转入转出`、`租借` 时填写；`续约`、`解约退役` 时省略。
- **`remark`** —— string，选填。转会状态/细节（如“报价遭拒，预计提价”“转会费2670万+400万浮动”“含买断条款”）。

规则：

- 一篇文章可含多位球员的转会，每位球员一条 transfer。
- `player`/`from`/`to` 的写法必须与 `entities` 中的同一实体写法一致（同一实体同一写法）。
- “被提及但未构成实际转会”的球员（如询价未果）只进 `entities`，不进 `transfers`。
- 教练更迭（教练上任/下课）**不在此列表**，见下方“coach_changes 说明”。

## coach_changes 说明

`coach_changes` 为**选填**，仅在 `theme.name = 转会与合同` 且子类含 `教练更迭` 时出现。它把“哪位教练、哪支球队、上任还是下课”结构化，是 `transfers` 的教练平行结构。

- **`type`** —— string，必填。教练变动方向：`上任` / `下课` / `续约` / `离任`（对应《主题.md》教练更迭“上任、下课、续约、合同到期离任”）。
- **`coach`** —— string，必填。教练名（全名），取值必须与 `entities` 中对应教练的 `name` 一致。
- **`team`** —— string，必填。涉及球队，取值必须与 `entities` 中对应球队的 `name` 一致。
- **`role`** —— string，选填。职位：`主教练` / `临时主教练` / `助理教练`，缺省视为主教练。
- **`remark`** —— string，选填。变动细节（如“接替被解雇的斯洛特”“签约至 2028 年”）。

规则：

- 一篇文章可含多位教练的变动，每位教练一条（如“伊劳拉上任 + 斯洛特下课”为两条）。
- `coach`/`team` 的写法必须与 `entities` 一致（同一实体同一写法）。
- 球员转会**不在此列表**，见上方“transfers 说明”。

## matches 说明

`matches` 为**选填**，当内容能确定**主队与客队**时输出，把“谁 vs 谁”的比赛关系结构化。它**不限主题**（比赛报道、深度分析、比赛预测等均可触发）、**无数量上限**，且与 `entities` 无逻辑关联（各自独立，与 `entities` 数量互不约束）。

- **`home`** —— string，必填。主队名（全称，遵循《实体.md》第 0 节）。
- **`away`** —— string，必填。客队名（全称）。
- **`event`** —— string，选填。赛事名（如“西班牙足球甲级联赛”）。
- **`referee`** —— string，选填。裁判名（全称）。
- **`remark`** —— string，选填。比赛细节（如“3-1”“第5轮”“因雨延期”）。

规则：

- **主客必填，否则无效**：缺 `home` 或 `away` 的比赛对象视为无效，**不关联**。
- **主客判定**：以“`vs` 左边即主队”为约定；中性场地（决赛等）用官方对阵表的行政主客。
- **主次优先**：标题/首段主要介绍的比赛 = 主比赛，只关联它；仅作对比/背景一笔带过的比赛忽略。并列无主次（如“史上10场逆风翻盘”盘点）才关联多场。
- **命名一致**：`home`/`away`/`event`/`referee` 若与 `entities` 中出现同一实体，写法必须一致（全称）。

## 示例

### 示例一：正常收录

```json
{
  "included": true,
  "theme": { "name": "深度分析", "sub": ["战术拆解"] },
  "entities": [
    { "type": "event", "name": "英格兰足球超级联赛", "importance": "required", "meta": { "season": "2026-27" } },
    { "type": "team", "name": "阿森纳", "importance": "required" },
    { "type": "team", "name": "曼彻斯特城", "importance": "required" },
    { "type": "player", "name": "大卫·拉亚·马丁", "importance": "significant", "meta": { "club": "阿森纳", "country": "西班牙" } },
    { "type": "coach", "name": "米克尔·阿尔特塔", "importance": "core", "meta": { "role": "主教练" } }
  ],
  "source": { "type": "媒体与记者", "reporter": "每日邮报", "quote": "据《每日邮报》报道，阿尔特塔正在改造阿森纳的高位逼抢体系" },
  "reason": "主类：命中《收录标准.md》深度分析·战术拆解·正面示例（对阵型与教练博弈的长篇拆解）；实体：按《组合规则.md》深度分析·战术拆解优先赛事+两队，另打教练阿尔特塔（战术主体）与门将拉亚（内容重点提及）"
}
```

### 示例二：不收录

```json
{
  "included": false,
  "theme": null,
  "entities": null,
  "reason": "不收录：球队发布伤病通告称全员健康，不构成新闻（《通用规则.md》第 1 条）"
}
```

### 示例三：转会（含 transfers）

```json
{
  "included": true,
  "theme": { "name": "转会与合同", "sub": ["转入转出"] },
  "entities": [
    { "type": "player", "name": "扬库巴·明特", "importance": "required", "meta": { "club": "布莱顿" } },
    { "type": "player", "name": "柯蒂斯·琼斯", "importance": "required", "meta": { "club": "利物浦" } },
    { "type": "team", "name": "利物浦", "importance": "required" },
    { "type": "team", "name": "布莱顿", "importance": "required" },
    { "type": "team", "name": "国际米兰", "importance": "required" }
  ],
  "transfers": [
    { "type": "转入转出", "player": "扬库巴·明特", "from": "布莱顿", "to": "利物浦", "remark": "5000万镑报价遭拒，预计提价" },
    { "type": "转入转出", "player": "柯蒂斯·琼斯", "from": "利物浦", "to": "国际米兰", "remark": "转会费2670万+400万浮动+10%二次分成" }
  ],
  "source": { "type": "媒体与记者", "reporter": "每日邮报", "quote": "据《每日邮报》报道，利物浦对扬库巴·明特提出 5000 万镑报价" },
  "reason": "主类：命中《收录标准.md》转会与合同·转入转出·正面示例；实体：按《组合规则.md》转入转出优先球员+涉及球队；transfers：明特（布莱顿→利物浦）、柯蒂斯·琼斯（利物浦→国际米兰）"
}
```

### 示例四：教练更迭（含 coach_changes）

```json
{
  "included": true,
  "theme": { "name": "转会与合同", "sub": ["教练更迭"] },
  "entities": [
    { "type": "coach", "name": "毛里西奥·波切蒂诺", "importance": "required", "meta": { "role": "主教练", "team": "切尔西" } },
    { "type": "team", "name": "切尔西", "importance": "required" }
  ],
  "coach_changes": [
    { "type": "上任", "coach": "毛里西奥·波切蒂诺", "team": "切尔西", "role": "主教练", "remark": "签约三年" }
  ],
  "reason": "主类：命中《收录标准.md》转会与合同·教练更迭·正面示例（“官方：波切蒂诺出任切尔西主帅”）；实体：按《组合规则.md》教练更迭优先教练+球队；coach_changes：波切蒂诺上任切尔西"
}
```

### 示例五：比赛（含 matches）

```json
{
  "included": true,
  "theme": { "name": "比赛报道", "sub": ["战报"] },
  "entities": [
    { "type": "event", "name": "英格兰足球超级联赛", "importance": "required", "meta": { "season": "2026-27" } },
    { "type": "team", "name": "利物浦", "importance": "required" },
    { "type": "team", "name": "托特纳姆热刺", "importance": "required" },
    { "type": "player", "name": "穆罕默德·萨拉赫", "importance": "core" }
  ],
  "matches": [
    { "home": "利物浦", "away": "托特纳姆热刺", "event": "英格兰足球超级联赛", "remark": "3-1" }
  ],
  "reason": "主类：命中《收录标准.md》比赛报道·战报·正面示例；实体：按《组合规则.md》战报优先赛事+主客两队，另打进球者穆罕默德·萨拉赫；matches：利物浦（主）vs 托特纳姆热刺（客）"
}
```

> 实体只打重点、去重、枚举合法性等校验约束见《通用规则.md》；机器校验见《返回结构.schema.json》。
